# Frontend Environment Variables (must be prefixed with VITE_)

# GraphQL API endpoint
VITE_GRAPHEOS_API_URI=http://localhost:4000

# Object storage URL for downloading files by hash
VITE_GRAPHEORS_OBJECT_STORAGE_URI=http://localhost:9000
